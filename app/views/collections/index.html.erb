<p id="notice" class="hidden"><%= notice %></p>

<%= render @collections %>

<div class="links-main-container">
	<% @links.each do |link| %>
		<%= render '/links/link' , link: link%>
	<% end %>
</div>

